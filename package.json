{
  "name": "@kimduumin/ncloud-mcp",
  "version": "0.1.3",
  "description": "Read-only MCP server for NAVER Cloud Platform inventory queries.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "ncloud-mcp": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "smoke": "npm run build && node scripts/read-only-policy-smoke.mjs",
    "test": "npm run smoke",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
